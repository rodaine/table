sudo: false
language: go
go: 1.5

cache:
  directories:
    - $GOPATH

install:
  - go get -t ./...
  - which golint || go get github.com/golang/lint/golint

script:
  - gofmt -l .
  - golint ./...
  - go tool vet -test .
  - go test -v -cover ./...
